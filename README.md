# ZIgsweeper
Terminal Minesweeper in Zig(0.15.2)


You can run the binaries directly with
eg.
chmod +x Snake
./Snake


this works only on linux, so far.
theres the binaries on the release section aswella s the repo
